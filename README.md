# Velociraptor - A framework for Geoloqi development

## Usage (for development)

Copy config.yml.template to config.yml

    $ gem install bundler
    $ bundle install
    $ ./start.sh

## Running the tests

    $ bundle exec rake test